# 📝 LSTM Next Word Prediction

A deep learning project that uses an LSTM neural network to predict the next word in a sequence of text. The model is trained on textual data and learns contextual relationships between words to generate accurate next-word predictions.
